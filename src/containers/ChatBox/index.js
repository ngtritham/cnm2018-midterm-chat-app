import React from 'react'
import History from './History';

export default (props) => {
    return (
        <div className="chat">
            {/* <Header friend={props.friend}/> */}
            <History/>
        </div>
    )
}
